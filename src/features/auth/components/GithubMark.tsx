/**
 * GitHub 공식 octicon `mark-github-16` 의 `d` 원문. `viewBox="0 0 16 16"` 와 같은 좌표계다.
 *
 * 🔴 **손으로 줄이거나 다시 적지 마라.** 이전 판은 이 문자열이 손상돼 `c` 명령 두 곳의
 * 인자가 **77개·46개**로 6의 배수가 아니었다 — 마이너스 부호가 빠져
 * `0 0-.67-.22-2.2.82` 가 `0 0.67-.21 2.2.82` 로 붙었다.
 * SVG 사양은 인자 수가 틀린 명령을 만나면 **그 지점까지만 그리고 뒤를 통째로 버리므로**,
 * 예외도 콘솔 경고도 없이 바깥 원의 왼쪽 절반만 남았다(bbox 폭 16 → **8**).
 * 운영 화면에서 보이던 잘림이 그것이다.
 *
 * 컴포넌트 밖에 두는 이유는 **시험이 읽기 위해서**다 — 렌더 없이 좌표계와 명령별 인자
 * 수를 셀 수 있다(`GithubMark.test.ts`).
 */
export const GITHUB_MARK_PATH =
  "M8 0c4.42 0 8 3.58 8 8a8.013 8.013 0 0 1-5.45 7.59c-.4.08-.55-.17-.55-.38 0-.27.01-1.13.01-2.2 0-.75-.25-1.23-.54-1.48 1.78-.2 3.65-.88 3.65-3.95 0-.88-.31-1.59-.82-2.15.08-.2.36-1.02-.08-2.12 0 0-.67-.22-2.2.82-.64-.18-1.32-.27-2-.27s-1.36.09-2 .27c-1.53-1.03-2.2-.82-2.2-.82-.44 1.1-.16 1.92-.08 2.12-.51.56-.82 1.27-.82 2.15 0 3.06 1.86 3.75 3.64 3.95-.23.2-.44.55-.51 1.07-.46.21-1.61.55-2.33-.66-.15-.24-.6-.83-1.23-.82-.67.01-.27.38.01.53.34.19.73.9.82 1.13.16.45.68 1.31 2.69.94 0 .67.01 1.3.01 1.49 0 .21-.15.45-.55.38A7.995 7.995 0 0 1 0 8c0-4.42 3.58-8 8-8Z";

/**
 * GitHub 공식 mark.
 *
 * 🔴 **아이콘 하나 때문에 브랜드 아이콘 라이브러리를 더하지 않는다**.
 * 지금 쓰는 `lucide-react` 에는 브랜드 아이콘이 없어 inline SVG 로 둔다 — 원본 형태를
 * 그대로 쓰고 비율·획을 변형하지 않는다.
 *
 * 🔴 **색을 박아 두지 않는다.** `currentColor` 라 버튼의 글자색을 따라가므로
 * Light·Dark 어느 쪽에서도 label 과 같은 대비를 갖는다.
 *
 * 장식이다 — 옆의 label 이 이미 「GitHub」을 말한다. `aria-hidden` 으로 접근성 이름이
 * 두 번 읽히지 않게 한다.
 */
export function GithubMark({ className }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 16 16"
      fill="currentColor"
      aria-hidden
      focusable="false"
      className={className}
    >
      <path d={GITHUB_MARK_PATH} />
    </svg>
  );
}
